# ARO HCP virt stack overview (Slidev)

Red Hat MOBB-themed deck for the two-checkout ARO HCP + OpenShift Virtualization pattern.

```bash
cd slides/overview
npm install
npm run dev
```

Build: `npm run build` (output in `dist/`, gitignored).
