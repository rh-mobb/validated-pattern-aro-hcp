# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info

# Local config and secrets (legacy paths)
config/cluster.env
config/cluster.tfvars
config/jump
config/jump.pub

# Operator tfvars (committed examples are un-ignored below)
*.tfvars
!clusters/public/terraform.tfvars
!clusters/private/terraform.tfvars
!clusters/aro-virt/terraform.tfvars

# Per-cluster operator artifacts
clusters/*/infrastructure.tfstate*
clusters/*/.terraform/
clusters/*/terraform.tfplan
clusters/*/jump
clusters/*/jump.pub
clusters/*/operator-hosts.snippet
clusters/*/sshuttle.pid
clusters/*/platform.json

# Kubeconfig and CLI artifacts
.kube/
*.kubeconfig
kubeconfig
*.whl

# Entra / external-auth generated files
manifest.json
.external-auth/

# Reference clones (local source material)
references/

# Superpowers plans/specs (local agent artifacts)
docs/superpowers/

# OS / editor
.DS_Store
.idea/
.vscode/
.cursor/
.history/
*.swp
*.swo

# Test artifacts
tests/bats/tmp/

# MkDocs
.venv-docs/
site/

# Slidev
slides/*/node_modules/
slides/*/dist/

# Red Hat pull secrets and operator scratch (never commit)
pull-secret.txt
/tmp/
